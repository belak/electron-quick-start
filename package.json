{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "scripts": {
    "serve": "parcel serve --target renderer ./src/index.html",
    "watch": "parcel watch --target main ./src/main.ts",
    "build:main": "parcel build --target main ./src/main.ts",
    "build:renderer": "parcel build --target renderer ./src/renderer.ts",
    "build:preload": "parcel build --target preload ./src/preload.ts",
    "dev": "electron ."
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^8.2.2",
    "parcel": "^2.0.0-nightly.212",
    "typescript": "^3.8.3"
  },
  "main": "build/main.js",
  "preload": "build/preload.js",
  "renderer": "build/renderer.js",
  "targets": {
    "main": {
      "context": "electron-main"
    },
    "preload": {
      "context": "electron-renderer"
    },
    "renderer": {
      "context": "electron-renderer"
    }
  }
}
