{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "scripts": {
    "serve": "parcel serve --target electron-renderer ./index.html",

    "watch:main": "parcel watch --no-hmr --target electron-main ./src/main.ts",
    "watch:renderer": "parcel watch --target electron-renderer ./index.html",
    "watch:preload": "parcel watch --no-hmr --target electron-preload ./src/preload.ts",

    "build:main": "parcel build --target electron-main ./src/main.ts",
    "build:renderer": "parcel build --target electron-renderer ./index.html",
    "build:preload": "parcel build --target electron-preload ./src/preload.ts",

    "dev": "electron ./build"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^8.2.2",
    "parcel": "^2.0.0-nightly.212",
    "typescript": "^3.8.3"
  },
  "electron-main": "build/index.js",
  "electron-preload": "build/preload.js",
  "electron-renderer": "build/index.html",
  "targets": {
    "electron-main": {
      "context": "electron-main"
    },
    "electron-preload": {
      "context": "electron-renderer"
    },
    "electron-renderer": {
      "context": "electron-renderer"
    }
  }
}
